#server:
#  port: 8081
#  servlet:
#    context-path: /resource-server-jwt
spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://localhost:9443/oauth2/token
