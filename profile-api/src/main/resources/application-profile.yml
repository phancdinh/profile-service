profile:
  datasource:
    mongoProperties:
      field-naming-strategy: org.springframework.data.mapping.model.SnakeCaseFieldNamingStrategy
      auto-index-creation: true
      uri: ${DATABASE_CONNECTION_URI}
      database: ${DATABASE_NAME}
  web-client: ${PROFILE_WEB_CLIENT}
